packages:
  - web

allowBuilds:
  esbuild: true
  better-sqlite3: true
  argon2: true
  # Optional native speedups of ssh2; pure-JS fallback is used (no build tools needed).
  ssh2: false
  cpu-features: false
